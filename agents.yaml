# agents.yaml — Kitty Collab Board

# NO API KEYS REQUIRED
# This board is for coordination only.
# You and I (Qwen) use our own AI tools separately.
# The board is just JSON files we both read/write.

agents:
  # Human Operator (You)
  - name: human
    role: operator
    team: null
    
  # Qwen (Me - collaborating via this chat)
  - name: qwen
    role: collaborator
    team: null
    
  # Claude (When you use Claude Code)
  - name: claude
    role: collaborator
    team: null
    
  # Copilot (When you use GitHub Copilot)
  - name: copilot
    role: collaborator
    team: null

# That's it. No API keys. No SDKs.
# We all use the board by reading/writing JSON files.
